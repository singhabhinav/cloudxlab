::script will install the node.js modules and start the server using script in package.json

::command to install the node.js modules related to the project.
::It refers the package.json to install the modules.
call npm install

::Command to start the server by referring the package.json to start the script.
npm start